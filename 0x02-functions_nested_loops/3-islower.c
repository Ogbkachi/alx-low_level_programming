#include "main.h"
/**
 * _islower - check if a char is lowercase
 * @c: first parameter
 *
 * Description: prints all lowercase letters
 * Return: Always (0)
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
